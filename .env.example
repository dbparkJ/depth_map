# VWorld WebGL 3.0 client key. The real .env file is intentionally ignored by Git.
VWORLD_API_KEY=YOUR_VWORLD_API_KEY

# Only set this when the VWorld key is restricted to a registered domain.
# VWORLD_DOMAIN=localhost

